desc: http test
runners:
  req:
    endpoint: https://httpbin.org/
debug: false
steps:
  - 
    desc: health check
    req:
      /:
        get:
          body: null
    test: |
      current.res.status == 200
